

{% block contenido -%}
    
    <div class="panel panel-default">
  <!-- Default panel contents -->
  <div class="panel-heading"<h1>Nuevo tipo de consulta</h1></div>
  <div class="panel-body">
  {{ form_start(form) }}
    {{ form_errors(form) }}
    <div class="row">
        <div class="col-xs-12 col-sm-4">
        {{ form_row(form.nombre) }}
        {#{{ form_row(form.estado) }}#}

        </div>
    </div>
    
             <div class="form-grouphide">
                <a href="{{ path('admin_tipoconsulta') }}" class="btn btn-default btn-sm" id="cancelarNuevo">Cancelar</a>
                {{ form_widget(form.submit) }}
              </div>
            
       
    {{ form_end(form) }}
  </div>


</div>        
{% endblock %}
