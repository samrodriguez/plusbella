{% extends '::base.html.twig' %}

{% block body -%}
    <h1>PersonaTratamiento</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Costoconsulta</th>
                <td>{{ entity.costoConsulta }}</td>
            </tr>
            <tr>
                <th>Numsesiones</th>
                <td>{{ entity.numSesiones }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_personatratamiento') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('admin_personatratamiento_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
