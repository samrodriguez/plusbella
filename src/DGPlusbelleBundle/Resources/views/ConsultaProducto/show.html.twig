{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ConsultaProducto</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Indicaciones</th>
                <td>{{ entity.indicaciones }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_consultaproducto') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('admin_consultaproducto_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
