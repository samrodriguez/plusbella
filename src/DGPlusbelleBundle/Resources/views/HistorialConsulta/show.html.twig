{% extends '::base.html.twig' %}

{% block body -%}
    <h1>HistorialConsulta</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Valordetalle</th>
                <td>{{ entity.valorDetalle }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_historialconsulta') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('admin_historialconsulta_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
