{% extends 'DGPlusbelleBundle:Layout:layout.html.twig' %}

{% block contenido -%}
    <h1>Historial Consulta list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Valor detalle</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('admin_historialconsulta_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.valorDetalle }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_historialconsulta_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_historialconsulta_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('admin_historialconsulta_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
