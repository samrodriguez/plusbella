{% extends 'DGPlusbelleBundle:Layout:general.html.twig' %}

{% block contenido -%}
    <h1>Paciente edit</h1>

    {{ form_start(edit_form) }}
    {{ form_errors(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_paciente') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
    {{ form_end(edit_form) }}
{% endblock %}
