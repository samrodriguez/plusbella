{% extends '::base.html.twig' %}

{% block body -%}
    <h1>SeguimientoTratamiento list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Numsesion</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('admin_seguimientotratamiento_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.numSesion }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_seguimientotratamiento_show', { 'id': entity.id }) }}">show</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    {% endblock %}
