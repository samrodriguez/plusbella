{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Bitacora list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Accion</th>
                <th>Fechaaccion</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('admin_bitacora_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.accion }}</td>
                <td>{% if entity.fechaAccion %}{{ entity.fechaAccion|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_bitacora_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_bitacora_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('admin_bitacora_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
