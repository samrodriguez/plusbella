{% extends '::base.html.twig' %}


{% block css %}
    {{parent()}}
    <style>
        #select2-dgplusbellebundle_cierreadministrativo_empleado-container{
            width: 400px;
        }
        .select2.select2-container.select2-container--default{
            width:400px;
        }
        
        select{
            width: 400px;
        }
    </style>
{% endblock %}

{% block body -%}
<div class="panel panel-default">
  <!-- Default panel contents -->
<div class="panel-heading"><p>Nuevo cierre administrativo</p></div>
<div class="panel-body">

    {{ form_start(form) }}
    {{ form_errors(form) }}
        
        <div class="col-md-3" style="margin-bottom:20px;">
            {{ form_widget(form.empleado) }}   
        </div>
        
        <div class="clearfix"></div>
        <div class="col-md-2">
            {{ form_row(form.fecha) }}
        </div>
        <div class="col-md-2">
            {{ form_row(form.horaInicio) }}
        </div>
        <div class="col-md-2">
            {{ form_row(form.horaFin) }}
        </div>
        <div class="col-md-6">
            {{ form_row(form.motivo) }}
        </div>
         
        {#{{ form_row(form.estado) }}#}
  
 
    
        <div class="form-grouphide col-sm-9">
            <a href="{{ path('admin_cierreadministrativo') }}" class="btn btn-default  btn-sm" id="cancelarNuevo">Cancelar</a>
            {{ form_widget(form.submit) }}
        </div>
   
    {{ form_end(form) }}
    </div>
</div>    
    
{% endblock %}
{% block javascripts_step %}
    <script>
        $(document).ready(function(){
                 var link=0;
                 var estadoPrevio = "";
                 
        }); 
    </script>
{%endblock%}