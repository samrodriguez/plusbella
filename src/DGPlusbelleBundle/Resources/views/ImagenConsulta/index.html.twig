{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ImagenConsulta list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Foto</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('admin_imagenconsulta_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.foto }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_imagenconsulta_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_imagenconsulta_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('admin_imagenconsulta_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
